{% extends "::layout.html.twig" %}

{% block title %}

	{{ title }}

{% endblock %}

{% block container %}
	
	{# ESTO ES UN COMENTARIO
	
	#}
	{% set nombre = "Henry" %}
	{{ nombre }}

	{{ parent() }}
	Hello World!
	<br/>
	{% for producto in productos %}
		{{ producto.producto }}	
	{% endfor %}

	<br/>
	{{ productos|length }}

	{# TRABAJANDO CON FECHAS #}
	{% set fecha = date("","America/Lima") %}
	{{ dump(fecha) }}

	<br/>
	{{ include("MiBundle:Default:partial.html.twig") }}
	{{ max([13, 87, 190, 54, 135]) }}

	<br/>
	{{ generateTable(5,7)|raw }}

{% endblock %}